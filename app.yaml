application: some-example
version: 1
runtime: python27
api_version: 1
threadsafe: yes

# Makes no difference whether this is there or not.
auto_id_policy: default

handlers:
- url: .*
  script: main.app

libraries:
- name: webapp2
  version: "2.5.2"
